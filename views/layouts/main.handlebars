<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css"
    />
    <link rel="stylesheet" href="/public/assets/css/burger_style.css" />
    <title>Eat Da Burger</title>
  </head>

  <body>
    <h1>Eat Da Burger</h1>
    <div class="container">
      {{!-- <!-- {{!Left div that contains burgers submitted from form }} --> --}}
      <div id="burgerstoeat">Burgers to Eat</div>
      {{!-- <!-- {{!Right div that conatins burgers that were eaten }} --> --}}
      <div id="eatenburgers">Burgers Eaten</div>
      {{!-- <!-- {{!Center div that contains a form with submit button }} --> --}}
      <div>
        <form>
          <div class="form-group">
            <label for="submitBurger">Create a Burger</label>
            <input
              type="textarea"
              class="form-control"
              id="burgerInput"
              placeholder="Enter your burger if you dare!"
            />
          </div>
          <button type="submit" class="btn btn-primary">Submit</button>
        </form>
      </div>
    </div>

    <!-- {{!Bootstrap CDN }} -->
    <script
      type="text/javascript"
      src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/js/bootstrap.min.js"
    ></script>
  </body>
</html>
